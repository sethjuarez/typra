name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Build, test, and pack
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm

      - name: Setup npm
        run: npm install -g npm@11

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: 1.22.x
          cache: false

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Swift
        uses: swift-actions/setup-swift@v3
        with:
          swift-version: "6.0"

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: false

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Generate fixtures
        run: npm run generate:fixtures

      - name: Validate generated fixtures
        run: npm run validate:fixtures

      - name: Validate npm package
        run: npm run pack:dry-run

  typespec-compatibility:
    name: TypeSpec ${{ matrix.compiler }} / json-schema ${{ matrix.json_schema }} / http ${{ matrix.http }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - compiler: "1.10.0"
            json_schema: "1.10.0"
            http: "1.10.0"
          - compiler: "1.15.0"
            json_schema: "1.15.0"
            http: "1.15.0"

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm

      - name: Setup npm
        run: npm install -g npm@11

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: 1.22.x
          cache: false

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Swift
        uses: swift-actions/setup-swift@v3
        with:
          swift-version: "6.0"

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: false

      - name: Configure matrix toolchain
        env:
          TYPESPEC_COMPILER_VERSION: ${{ matrix.compiler }}
          TYPESPEC_JSON_SCHEMA_VERSION: ${{ matrix.json_schema }}
          TYPESPEC_HTTP_VERSION: ${{ matrix.http }}
        run: |
          npm pkg set "devDependencies.@typespec/compiler=$TYPESPEC_COMPILER_VERSION"
          npm pkg set "devDependencies.@typespec/json-schema=$TYPESPEC_JSON_SCHEMA_VERSION"
          npm pkg set "overrides.@typespec/compiler=$TYPESPEC_COMPILER_VERSION"
          npm pkg set "overrides.@typespec/json-schema=$TYPESPEC_JSON_SCHEMA_VERSION"
          npm pkg set "overrides.@typespec/http=$TYPESPEC_HTTP_VERSION"
          npm pkg set --workspace @typra/emitter "peerDependencies.@typespec/compiler=$TYPESPEC_COMPILER_VERSION"
          npm pkg set --workspace @typra/emitter "peerDependencies.@typespec/http=$TYPESPEC_HTTP_VERSION"
          npm pkg set --workspace @typra/emitter "peerDependencies.@typespec/json-schema=$TYPESPEC_JSON_SCHEMA_VERSION"
          npm pkg set --workspace @typra/emitter "devDependencies.@typespec/compiler=$TYPESPEC_COMPILER_VERSION"
          npm pkg set --workspace @typra/emitter "devDependencies.@typespec/http=$TYPESPEC_HTTP_VERSION"
          npm pkg set --workspace @typra/emitter "devDependencies.@typespec/json-schema=$TYPESPEC_JSON_SCHEMA_VERSION"

      - name: Install dependencies
        run: npm install --package-lock=false

      - name: Verify matrix toolchain
        env:
          EXPECTED_COMPILER: ${{ matrix.compiler }}
          EXPECTED_JSON_SCHEMA: ${{ matrix.json_schema }}
          EXPECTED_HTTP: ${{ matrix.http }}
        run: |
          node -e 'const compiler = require("./node_modules/@typespec/compiler/package.json").version; const schema = require("./node_modules/@typespec/json-schema/package.json").version; const http = require("./node_modules/@typespec/http/package.json").version; if (compiler !== process.env.EXPECTED_COMPILER || schema !== process.env.EXPECTED_JSON_SCHEMA || http !== process.env.EXPECTED_HTTP) { console.error(`Expected TypeSpec ${process.env.EXPECTED_COMPILER} / json-schema ${process.env.EXPECTED_JSON_SCHEMA} / http ${process.env.EXPECTED_HTTP}, got ${compiler} / ${schema} / ${http}`); process.exit(1); }'

      - name: Build
        run: npm run build

      - name: Generate fixtures
        run: npm run generate:fixtures

      - name: Validate generated fixtures
        run: npm run validate:fixtures
